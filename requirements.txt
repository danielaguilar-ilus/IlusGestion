flask>=3.0.0
requests>=2.31.0
pymysql>=1.1.0
DBUtils>=3.0.0
# Playwright: PDFs de etiquetas + módulo Etiquetas (módulo Etiquetas y transporte).
# Pineado >= 1.50.0 — esta versión usa greenlet 3.x que SÍ compila en Python 3.13.
playwright>=1.50.0
# greenlet pineado >= 3.1.2 porque 3.1.1 (dep transitiva de playwright)
# NO compila en Python 3.13 (CPython renombró c_recursion_remaining a
# py_recursion_remaining en TPythonState.cpp). Sin este pin el build de
# Cloud Build buildpacks falla en clean install.
greenlet>=3.1.2
cloudinary>=1.36.0
google-cloud-storage>=2.14.0
google-cloud-translate>=3.15.0
openpyxl>=3.1.0
gunicorn>=21.2.0
python-docx>=1.1.0
pdfplumber>=0.10.0
pypdf>=4.0.0
# pypdfium2: renderizador PDF→imagen AUTOCONTENIDO (trae el binario PDFium en
# el wheel, NO necesita poppler de sistema). Lo usa _fedex_label_to_png_datauri
# para rasterizar etiquetas FedEx a 4×6 en el build de Cloud Run (nixpacks NO
# instala poppler-utils; el Dockerfile sí, pero prod usa --source=buildpacks).
pypdfium2>=4.30.0
# OCR determinista (sin IA) para contratos escaneados — necesita tesseract + poppler (Dockerfile)
pytesseract>=0.3.10
pdf2image>=1.17.0
Pillow>=10.0.0
anthropic>=0.30.0
twilio>=9.10.0
# Random ERP — SQL Server read-only via pymssql
pymssql>=2.3.0
